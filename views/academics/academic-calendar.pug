extends ../layouts/base.pug

block content
    //- include ../layouts/banner.pug

    main.page-layout 
        include ../layouts/pagemenu.pug

        .main
            div
                h1.page-heading Academic Calendar

                p=academicCalendar.description
                .line-above-heading
                if academicCalendar && academicCalendar.pdf_filepath
                    iframe.pdf-viewer.margin-top-xs.hidden(src=`${academicCalendar.pdf_filepath}`)
                    .pdf.margin-top-sm.hidden
                        .pdf__heading
                        .pdf__container
                        .pdf__download
                            a(href=`${academicCalendar.pdf_filepath}` target="_blank")
                                button Download
                else
                    p No academic calendar available
    script.
        pdfJs(["#{academicCalendar.pdf_filepath}"])